import Container from "../../layout/Container";
import { StyledFooter } from "./footer.styles";

export default function Footer() {
	return (
		<StyledFooter>
			<Container>
				<p>A footer will arrive here.</p>
				<p>And some info will arrive here.</p>
			</Container>
		</StyledFooter>
	);
}
